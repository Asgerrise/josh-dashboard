import React from "react";
import Nav from "./Nav";

const Header = () => {
  return (
    <header className="header">
      <div className="logo">F.</div>
      <Nav />
    </header>
  );
};

export default Header;
